import React from 'react';
import ReactDOM from 'react-dom/client';
import { AvatarWidget } from './avatarWidget';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(<AvatarWidget />);
